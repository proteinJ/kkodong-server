services:
  redis:
    image: redis:latest
    container_name: pawwalk-redis
    ports:
      - "6379:6379"
    restart: unless-stopped
    volumes:
      - pawwalk-redis-data:/data

volumes:
  pawwalk-redis-data:
